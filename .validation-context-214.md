Validation-only surface for upstream PR #214.

Production bytes under test are the exact `d5c85dd147df5db65fe71b21fa14554f6431672c` contributor head. This branch adds only inert validation markers so a fresh `pull_request` event can execute native CI after a workflow-token push suppressed recursive Actions.

Do not promote either `.validation-*` file upstream.
